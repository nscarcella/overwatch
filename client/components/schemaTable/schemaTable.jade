template(name='schemaTable')
	.schemaTable
		+reactiveTable(settings=innerTableSettings)

template(name='emptySchemaTable')
	.schemaTableEmpty
		p No entries to show

template(name='actionsCell')
		each actions
			i(class='action #{this}' action='#{this}' title='#{this}')